random_seed: 42
train_cutoff_year: 2023
val_year: 2023
test_year: 2024

data:
  seq_len: 12
  pred_len: 12

lstm:
  hidden: 32
  layers: 2
  epochs: 300
  learning_rate: 0.01

transformer:
  d_model: 64
  nhead: 8
  num_layers: 2
  dim_feedforward: 128
  epochs: 500
  learning_rate: 0.001

paths:
  weights_dir: model/weights
