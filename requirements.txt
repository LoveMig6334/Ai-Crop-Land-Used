# This file points to the full development environment.
# See requirements-core.txt for the minimal ML-only dependencies.
-r requirements-dev.txt

# PyTorch CUDA install (run manually, replacing requirements-core.txt torch lines):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# Note: pywin32 and pywinpty are Windows-only; Jupyter installs them automatically on Windows.
